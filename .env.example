# ACP Sidecar（Tauri 桌面端自动启动，无需单独跑 HTTP 服务）

# Agent 工作目录（opencode acp --cwd）
# VITE_ACP_CWD="D:/your-project"

# Agent 可执行文件，默认 opencode
# VITE_ACP_AGENT_COMMAND="opencode"

# Agent 参数 JSON 数组，默认 ["acp"]（stdio 模式，不要加 --port）
# VITE_ACP_AGENT_ARGS='["acp"]'
